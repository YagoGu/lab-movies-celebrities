<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=<device-width>, initial-scale=1.0">
    <title>Edit Movie</title>
</head>
<body>
    <h2>Edit a movie</h2>
    <form action="/movies/{{foundedMovie._id}}/edit" method="POST">
    
        <label for="title">Title</label>
        <input type="text" name="title" id="title" value="{{foundedMovie.title}}">

        <label for="genre">Genre</label>
        <input type="text" name="genre" id="genre" value="{{foundedMovie.genre}}">

        <label for="plot">Plot</label>
        <input type="text" name="plot" id="plot" value="{{foundedMovie.plot}}">

        <label for="celebrity">Celebrity</label>
        <select name="celebrity" multiple>
                {{#each foundedMovie.cast}}
                <option value="{{this._id}}">{{this.name}}</option>
                {{/each}}
        </select>

        <button type="submit">Edit a Movie</button>
    </form>
</body>
</html>